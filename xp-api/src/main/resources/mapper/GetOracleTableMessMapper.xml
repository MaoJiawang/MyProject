<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<!--查单表所有的存储过程 用于html页面展示-->
<mapper namespace="com.xp.oracle.mapper.GetOracleTableMess">
    <select id="getOracleTableMess" resultType="map" parameterType="string">
        SELECT * FROM 医院信息 a where 1=1
    </select>
</mapper>